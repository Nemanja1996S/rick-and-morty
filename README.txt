To start this project you need:
1.clone it on your machine
2.execute "npm install" command inside this project
3.execute "npm run dev"

To run unit tests in CLI: 
-execute "npm run test" command inside this project

To run unit tests in browser:
-execute "npm run test:ui" command inside this project